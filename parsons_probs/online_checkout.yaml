problem_name: online_checkout

problem_description: |
  <code>online_checkout</code>
    returns the checkout status message.
    If logged in and items in cart: "Proceeding to checkout"
    If logged in but empty cart: "Your cart is empty"
    If not logged in: "Please log in first"

  <pre><code>
  >>> online_checkout(True, 3)
  'Proceeding to checkout'
  >>> online_checkout(True, 0)
  'Your cart is empty'
  >>> online_checkout(False, 3)
  'Please log in first'
  </code></pre>

code_lines: |
  def online_checkout(logged_in, items_in_cart): #0given
  if !BLANK:
  if items_in_cart !BLANK 0:
  return "Proceeding to checkout"
  else:
  return "Your cart is empty"
  else:
  return "Please log in first"

test_fn: online_checkout
