def speed_check(speed, limit):
    """ Returns "Stop" if speed is above the limit, "Go ahead" otherwise.

    >>> speed_check(75, 45)
    'Stop'
    >>> speed_check(25, 45)
    'Go ahead'
    >>> speed_check(45, 45)
    'Go ahead'
    """
