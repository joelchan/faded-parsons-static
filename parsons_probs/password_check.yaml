problem_name: password_check

problem_description: |
  <code>password_check</code>
    returns "Warning: password is too short!" if password is less than
    8 characters, None otherwise.

  <pre><code>
  >>> password_check("abc")
  'Warning: password is too short!'
  >>> password_check("short")
  'Warning: password is too short!'
  >>> password_check("longenough")
  >>> password_check("12345678")
  </code></pre>

code_lines: |
  def password_check(password): #0given
  if !BLANK(password) < !BLANK:
  return "Warning: password is too short!"

test_fn: password_check
