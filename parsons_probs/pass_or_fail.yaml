problem_name: pass_or_fail

problem_description: |
  <code>pass_or_fail</code>
    returns "Pass" if score is 60 or above, "Fail" otherwise.

  <pre><code>
  >>> pass_or_fail(85)
  'Pass'
  >>> pass_or_fail(60)
  'Pass'
  >>> pass_or_fail(55)
  'Fail'
  >>> pass_or_fail(0)
  'Fail'
  </code></pre>

code_lines: |
  def pass_or_fail(score): #0given
  if score !BLANK 60:
  return "Pass"
  else:
  return "Fail"

test_fn: pass_or_fail
