def pass_or_fail(score):
    """ Returns "Pass" if score is 60 or above, "Fail" otherwise.

    >>> pass_or_fail(85)
    'Pass'
    >>> pass_or_fail(60)
    'Pass'
    >>> pass_or_fail(55)
    'Fail'
    >>> pass_or_fail(0)
    'Fail'
    """
